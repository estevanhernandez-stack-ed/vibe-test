#!/usr/bin/env node
/**
 * @esthernandez/vibe-test-cli — headless CLI entry point.
 *
 * Implementation deferred to checklist item #10 (Runtime hooks + CLI package).
 * This skeleton exists so the workspace dependency graph resolves during item #1.
 */

/* eslint-disable no-console */
console.error(
  'vibe-test-cli: v0.2 CLI implementation lands in checklist item #10.\n' +
    'For now, invoke /vibe-test:<command> inside a Claude Code session.',
);
process.exit(2);
