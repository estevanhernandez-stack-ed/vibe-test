# Proposed Changes — 2026-06-09

Generated by `/vibe-test:evolve` session `6b0d5b08-efdd-4833-abd4-41f17e325a1c`.

> **Prior art:** an external design review dated 2026-05-22 lives at
> `vibe-plugins/drafts/vibe-test/proposed-changes.md` (marketplace repo archive). It is
> not evolve output, but its meta-observation — that an evolve run mid-refactor had no
> valid write target — is cited as evidence by Proposal 02 below. Design-level unlocks
> from that review (mutation testing, ASVS threshold calibration, orchestrator framing)
> are out of evolve's SKILL-prose lane and stay on file there.

## Run context

- **Window:** last 30 days (2026-05-10 → 2026-06-09) contains **zero** log entries. All
  existing signal (1 friction, 1 win, 1 session) is dated 2026-04-17 — a synthetic smoke
  run (`project: smoke-SMOKE-1776464458363`). Per builder instruction the run proceeded
  with the window widened to all-time; **every weight below is stale-window and
  synthetic-source flagged.** Proposal 03 makes this branch a defined behavior.
- **Vitals pre-flight:** 2 ✓ · 4 ⚠ · 1 ✗ — house is **not** clean.
  - ✓ #1 SKILL cross-references (all `skills/*/SKILL.md` refs resolve; non-SKILL path
    drift noted under Proposal 05)
  - ⚠ #2 Templates (6 orphan templates on disk — referenced from `src/` handoff
    writers, not SKILL prose; benign)
  - ⚠ #3 Builder profile (`plugins.vibe-test` block absent — first-write pending;
    profile parses)
  - ⚠ #4 Anchored complements (`tessl:analyzing-test-coverage` absent; `playwright`
    present via MCP plugin; remaining 5 present)
  - ⚠ #5 Log volume (0 terminal entries in window < 3 — volume eval skipped)
  - ✗ #6 Friction-trigger consistency (8 non-canonical friction-type invocations
    across fix/gate/posture/coverage SKILL tables — Proposal 04)
  - ✓ #7 State-file schema integrity (4/4 JSONL lines parse, `schema_version: 1`)
  - The default on ✗ is abort; the builder's standing instruction for this run was the
    explicit override. Logged in the session terminal entry.
- **Below-threshold signals (excluded, not forgotten):**
  - `(audit, classification_mismatch)` — 1 entry, confidence `low`, weighted sum
    **0.3** < 1.8 threshold and < 3-entry minimum. Synthetic (smoke marker) besides.
    No proposal.
- **Pattern #14 ledger:** `audit` baseline = 0 clean runs (its one session carries a
  friction entry) + 1 `working_as_designed` win → baseline_strength **1.0** (< 5 — no
  demotion power, no behavior-change support either). **Demoted proposals: 0.**
  Consequence: no real-usage evidence supports changing any command's *behavior* —
  all five proposals below are structural/meta, grounded in repo and vitals evidence,
  and none touch audit/generate/fix/coverage/gate/posture core flows.
- **Human release action flagged (outside evolve's write authority):** tag
  `vibe-test-v0.2.4` shipped with `.claude-plugin/plugin.json` still reading `0.2.3`,
  and HEAD still reads `0.2.3`. Accepting Proposal 01 adds the guard; the slip itself
  needs a release decision — bump the manifest and roll forward (v0.2.5) or re-cut.

---

## Disposition — all five applied 2026-06-09

The five proposals this run generated were reviewed, ratified, and applied the same day.
Per the evolve convention, each proposal entry has **moved to
[`applied-changes.md`](./applied-changes.md)** — the full observation, diff,
justification, and evidence are preserved there alongside the commit that applied it.
Nothing is pending; this file is working memory and now holds only the run context above.

| Proposal | Title | Commit |
|----------|-------|--------|
| 2026-06-09-01 | Vitals Check #8 — release consistency (tag ↔ manifest) | `94a7e46` |
| 2026-06-09-02 | Evolve prerequisite — verify the solo repo by remote identity | `56bc15f` |
| 2026-06-09-03 | Evolve input hygiene — smoke-entry filter + stale-window guard | `ad4aa4d` |
| 2026-06-09-04 | Re-align fix/gate/posture/coverage friction tables to the map | `0e89a8d` |
| 2026-06-09-05 | Vitals Runtime Paths — point command files where they live | `db0d35f` |

**Release follow-through:** the human release action this run flagged (the
`vibe-test-v0.2.4` ↔ `0.2.3` manifest slip) was resolved by rolling the manifest
forward to **0.2.5** (skipping the mislabeled 0.2.4) in `bf3a4f5`, with a matching
CHANGELOG entry. Not tagged, not pushed — that remains a deliberate release step.
